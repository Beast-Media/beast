version: '3'
services:
  beast-database:
    image: "postgres"
    restart: always
    environment:
      POSTGRES_USER: beast
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: beast
    ports:
      - 1111:5432
    volumes:
      - database-volume:/var/lib/postgresql/data
  # beast:
  #   build:
  #     args:
  #       - API_PORT=3000
  #       - API_WS_PORT=3001
  #     context: .
  #   ports:
  #     - 1000:80
  #     - 3000:3000
  #   env_file: .env.docker
  #   volumes:
  #     - ./volumes:/volumes
  #     - /mnt/share:/mnt/share

volumes:
  database-volume:

